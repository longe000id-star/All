[← AI_Index](../../AI_Index.md) 

你的目的是：

> **每次对话中，我希望能拿到 Claude 处理问题时生成的所有中间脚本和输出文件，而不只是看到结果。这样我可以：**
> - 审查 Claude 的验证逻辑是否正确
> - 保存脚本以便日后复用或修改
> - 完整追踪 Claude 的工作过程，而不是只看到终端输出

所以完整提示词建议改成：

---

> 在这次对话中，对于你执行的每一个任务，请遵守以下规则：
> 1. 所有脚本和代码都必须先用 `create_file` 保存成 `.py`（或对应格式）的文件，不允许用 `python3 -c "..."` 直接执行
> 2. 所有生成的文件最终都必须复制到 `/mnt/user-data/outputs/`（这是 Claude.ai 唯一能让我下载文件的目录）
> 3. 每次回答结束时，用 `present_files` 把本次任务生成的所有文件提供给我下载
>
> **目的：我希望能完整追踪你的工作过程，审查验证逻辑，并保存所有脚本以便复用。**

---

这样以后新开对话粘贴进去就行了。
